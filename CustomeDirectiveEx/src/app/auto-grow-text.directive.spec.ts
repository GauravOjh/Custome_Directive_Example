import { AutoGrowTextDirective } from './auto-grow-text.directive';

describe('AutoGrowTextDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoGrowTextDirective();
    expect(directive).toBeTruthy();
  });
});
