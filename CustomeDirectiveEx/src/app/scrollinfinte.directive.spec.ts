import { ScrollinfinteDirective } from './scrollinfinte.directive';

describe('ScrollinfinteDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollinfinteDirective();
    expect(directive).toBeTruthy();
  });
});
